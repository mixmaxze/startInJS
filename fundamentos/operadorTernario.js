const resultado = nota => nota >= 7  ? 'Aprovado' : 'Reprovado' //tres operandos

// (true ou false) ? <caso true, faca isso> : <caso false, faca isso>

console.log(resultado(7.1))
console.log(resultado(6.9))

const status = nota >= 7  ? 'Aprovado' : 'Reprovado'